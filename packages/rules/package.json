{
  "name": "@unused/rules",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --root ../..",
    "dev": "tsc -w",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "typescript": "latest"
  },
  "dependencies": {
    "@unused/graph": "^0.0.0"
  }
}
